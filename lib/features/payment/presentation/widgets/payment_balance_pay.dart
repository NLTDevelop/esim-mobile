import 'package:flutter/material.dart';

class PaymentBalancePay extends StatelessWidget {
  const PaymentBalancePay({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
